diff --git a/node_modules/react-native-notificated/lib/module/core/hooks/useDrag.js b/node_modules/react-native-notificated/lib/module/core/hooks/useDrag.js
index 6e0b03a..35bcad2 100644
--- a/node_modules/react-native-notificated/lib/module/core/hooks/useDrag.js
+++ b/node_modules/react-native-notificated/lib/module/core/hooks/useDrag.js
@@ -1,11 +1,15 @@
 import { useCallback } from 'react';
-import { State } from 'react-native-gesture-handler';
-import { useAnimatedGestureHandler, useAnimatedStyle, useSharedValue, withSpring } from 'react-native-reanimated';
+import { Gesture } from 'react-native-gesture-handler';
+import { useAnimatedStyle, useSharedValue, withSpring, runOnJS } from 'react-native-reanimated';
 import { shouldTriggerGesture } from '../gestures/shouldTriggerGesture';
+
 export const useDrag = config => {
   const x = useSharedValue(0);
   const y = useSharedValue(0);
+  const ctxX = useSharedValue(0);
+  const ctxY = useSharedValue(0);
   const directions = getDragDirections(config.direction);
+  
   const resetDrag = useCallback(() => {
     x.value = withSpring(0, {
       mass: 0.2
@@ -14,33 +18,33 @@ export const useDrag = config => {
       mass: 0.2
     });
   }, [x, y]);
-  const dragGestureHandler = useAnimatedGestureHandler({
-    onStart: (_, ctx) => {
-      ctx.x = x.value;
-      ctx.y = y.value;
-    },
-    onActive: (_ref, ctx) => {
-      let {
-        translationX,
-        translationY
-      } = _ref;
-      x.value = ctx.x + translationX * directions.x;
-      y.value = ctx.y + translationY * directions.y;
-    },
-    onEnd: () => {
+  
+  const dragGestureHandler = Gesture.Pan()
+    .onBegin(() => {
+      'worklet';
+      ctxX.value = x.value;
+      ctxY.value = y.value;
+    })
+    .onUpdate((event) => {
+      'worklet';
+      x.value = ctxX.value + event.translationX * directions.x;
+      y.value = ctxY.value + event.translationY * directions.y;
+    })
+    .onEnd(() => {
+      'worklet';
       x.value = withSpring(0, {
         mass: 0.2
       });
       y.value = withSpring(0, {
         mass: 0.2
       });
-    }
-  });
+    });
+  
   const dragStateHandler = useCallback((onDragSuccess, onDragFail) => event => {
     const {
       nativeEvent
     } = event;
-    if (nativeEvent.state !== State.END) return event;
+    if (nativeEvent.state !== 5) return event; // State.END = 5
     const dragTriggered = shouldTriggerGesture(config, {
       distance: [nativeEvent.translationX, nativeEvent.translationY],
       velocity: [nativeEvent.velocityX, nativeEvent.velocityY]
@@ -48,6 +52,7 @@ export const useDrag = config => {
     if (dragTriggered) onDragSuccess();else onDragFail();
     return event;
   }, [config]);
+  
   const dragStyles = useAnimatedStyle(() => ({
     transform: [{
       translateX: x.value
@@ -55,6 +60,7 @@ export const useDrag = config => {
       translateY: y.value
     }]
   }));
+  
   return {
     dragGestureHandler,
     dragStateHandler,
