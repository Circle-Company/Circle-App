import activity_indicator from "./components/loading-activity_indicator"
import container from "./components/loading-container"

export const Loading = {
    Container: container,
    ActivityIndicator: activity_indicator,
}