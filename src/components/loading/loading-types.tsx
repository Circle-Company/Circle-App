export type LoadingProps = {
    from_color?: string,
    to_color?: string,
    size?: number,
    interval?: number
}
export type LoadingContainerProps = {
    width?: number,
    height?: number,
    children: React.ReactNode
}
