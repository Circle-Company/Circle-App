export type LoadingProps = {
    color?: string
    borderColor?: string
    size?: number,
    interval?: number
}
export type LoadingContainerProps = {
    width?: number,
    height?: number,
    children: React.ReactNode
}
